# Testing and Verification

Canonical operator/testing guidance now lives in `README.md`:

- `Testing` for local contributor verification commands (`make lint`, `make test-short`, `make test`).
- `Troubleshooting` for local verification flows and command usage.
- `Operations` for runtime, recovery, and support procedures.

CI smoke execution details are in `.github/workflows/ci.yml` (`smoke` job).
